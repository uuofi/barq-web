# معمارية الموقع التعريفي (delivery-website)

> وثيقة القرارات المعمارية. تشرح **لماذا** بُني كل شيء بهذا الشكل، لا ما هو موجود فقط — الكود يقول ما هو موجود.

---

## 1. المبادئ الحاكمة الستة

كل قرار في هذا المشروع مشتق من واحد من هذه:

1. **مصدر واحد للحقيقة (Single Source of Truth).** كل حقيقة مكتوبة في مكان واحد: الروابط في `paths.ts`، الإعدادات في `env.ts`، الألوان في `tokens.css`، مسارات الـ API في `endpoints.ts`. التكرار يُسمح به فقط عبر حدود النشر (مع تعليق يوضّح المرآة).

2. **الفشل مبكراً وبصوت عالٍ.** إعداد ناقص يرمي خطأً وقت الإقلاع. استجابة API بشكل غير متوقع تُرفض عند الحدود لا بعد ثلاث طبقات. موقع مكسور بوضوح أفضل من موقع مكسور بصمت.

3. **الشرائح العمودية (Feature Slices).** الميزة تملك كل طبقاتها: schema + api + hook + page في مجلد واحد. حذف ميزة = حذف مجلد.

4. **الاتجاه أحادي.** `page → hook → api → http`. لا تقفز الصفحة فوق الـ hook لتنادي الـ API مباشرة، ولا يعرف الـ API شيئاً عن React.

5. **العرض منفصل عن البنية.** لا يوجد أي قرار بصري في ملفات `.tsx` — فقط عناصر دلالية و`data-*` hooks. التصميم يأتي لاحقاً بلا إعادة هيكلة.

6. **العربية RTL هي الأصل.** ليست ترجمة لاحقة: `ar` هي `fallbackLng`، والاتجاه يُطبَّق قبل أول رسم، وكل CSS يستخدم خصائص منطقية (`inline-start`) لا `left/right`.

---

## 2. المكدس التقني ولماذا

| الطبقة | الاختيار | السبب |
|--------|---------|-------|
| البناء | Vite 5 | نفس أداة لوحة الأدمن — اتساق في المستودع |
| اللغة | TypeScript صارم | `noUncheckedIndexedAccess` مفعّل: `array[i]` يُعيد `T \| undefined` |
| التوجيه | React Router 6 (Data Router) | `errorElement` على مستوى المسار — لا شاشة بيضاء |
| بيانات الخادم | TanStack Query 5 | كاش + سياسة إعادة محاولة مركزية |
| النماذج | React Hook Form + Zod | نفس الـ schema يخدم التحقق والأنواع |
| الترجمة | i18next | يطابق تطبيق الموبايل |
| التنسيق | CSS Modules + متغيرات CSS | **بلا Tailwind** — انظر أدناه |
| الاختبار | Vitest + Testing Library | يشارك تهيئة Vite |

### لماذا لا Tailwind هنا (مع أن الأدمن يستخدمه)؟

المطلوب صريح: **معمارية بلا تصميم**. Tailwind ينقل قرارات التصميم إلى داخل الـ JSX — وهو ما يجعل «البناء بلا تصميم» مستحيلاً عملياً، ويجبر على إعادة كتابة كل مكوّن عند بدء التصميم. متغيرات CSS + CSS Modules تُبقي الـ JSX خالياً تماماً من الأنماط: عند بدء التصميم تُضاف ملفات `.module.css` بجانب المكوّنات ولا يتغيّر سطر واحد من المنطق.

إذا قررتم لاحقاً استخدام Tailwind، الانتقال رخيص لأن `tokens.css` يصلح مباشرةً كـ `theme` في `tailwind.config.js`.

---

## 3. خريطة المجلدات

```
src/
├── main.tsx                 نقطة الدخول: أنماط عامة + mount
├── App.tsx                  الجذر: providers + router فقط
│
├── app/                     تركيب التطبيق (composition root)
│   ├── providers/
│   │   ├── AppProviders.tsx     ترتيب الموفّرات — الترتيب مقصود ومشروح
│   │   └── ConsentProvider.tsx  موافقة الكوكيز = بوابة التحليلات
│   └── router/
│       ├── paths.ts             ⭐ كل رابط في الموقع
│       ├── routes.tsx           ⭐ سجل المسارات + بيانات SEO + الأعلام
│       ├── index.tsx            بناء الراوتر + Suspense + حدود الخطأ
│       ├── FeatureGate.tsx      مسار معطّل يُعرض لا يُرمى 404
│       └── RouteErrorBoundary.tsx
│
├── config/
│   ├── env.ts               ⭐ الإعدادات مُتحقَّق منها بـ Zod
│   ├── site.ts              هوية العلامة (ثابتة عبر البيئات)
│   └── constants.ts         مرآة ثوابت الباك اند (محافظات، حالات)
│
├── lib/                     منطق لا يعرف React (باستثناء seo/)
│   ├── http/
│   │   ├── apiClient.ts     Axios + تطبيع الأخطاء + تحقق Zod عند الحدود
│   │   ├── RequestError.ts  شكل الخطأ الوحيد الذي تراه الواجهة
│   │   └── endpoints.ts     ⭐ سجل المسارات: live مقابل planned
│   ├── seo/schema.ts        بناة JSON-LD (دوال نقية)
│   ├── utils/               format · cn · url
│   ├── queryClient.ts       سياسة الكاش وإعادة المحاولة
│   ├── queryKeys.ts         مصنع مفاتيح الكاش الهرمي
│   ├── analytics.ts         واجهة محايدة عن المزوّد + طابور + بوابة موافقة
│   ├── storage.ts           localStorage آمن (لا ينهار في الوضع الخاص)
│   └── logger.ts            منفذ واحد للأخطاء (مكان Sentry مستقبلاً)
│
├── features/                ⭐ شرائح عمودية
│   ├── tracking/            المرجع الكامل: schema → api → hook → page
│   ├── contact/             schema + api + mutations (نماذج)
│   ├── home · merchants · drivers · about · coverage · faq · download
│   ├── legal/               LegalPage مشترك + terms + privacy
│   └── errors/NotFoundPage.tsx
│
├── components/
│   ├── layout/              RootLayout · Header · Footer · Container · ScrollToTop
│   ├── feedback/            ErrorBoundary · PageLoader · QueryState
│   └── seo/Seo.tsx          ⭐ مدير الـ head بلا مكتبات
│
├── hooks/                   usePageView · useLocale · useConsent · useDebouncedValue · useMediaQuery
├── i18n/                    تهيئة + ar.json + en.json
├── styles/                  reset.css → tokens.css → global.css
├── types/api.ts             أشكال الظرف والترقيم المشتركة
└── test/                    setup.ts + architecture.test.tsx
```

---

## 4. القرارات المعمارية الرئيسية

### 4.1 سجل المسارات كمصدر حقيقة (`app/router/routes.tsx`)

كل مسار يُصرَّح **ببياناته الوصفية** لا بمكوّنه فقط:

```ts
{
  path: paths.contact,
  titleKey: 'nav.contact',
  sitemap: { include: true, priority: 0.5 },
  enabled: env.features.leadForms,   // ← عَلَم ميزة
  inMainNav: true,
  element: ContactPage,
}
```

النتيجة المباشرة: **قائمة التنقل و sitemap.xml و noindex كلها مشتقة من هذا السجل**. صفحة معطّلة تختفي من القائمة وتخرج من الـ sitemap تلقائياً — لا توجد قائمة ثانية تُنسى.

### 4.2 مسار معطّل يُعرض ولا يُرمى (`FeatureGate`)

الميزة التي لا يدعمها الباك اند بعد **لا تُحذف** من الراوتر. المسار يُحلّ ويعرض حالة «قريباً» مع `noIndex`. السبب: حذف المسار يعني 404 لأي رابط خارجي موجود، وعرضه بلا `noIndex` يعني فهرسة صفحة فارغة — وكلاهما ضرر حقيقي في السيو.

### 4.3 التحقق عند الحدود (`lib/http/apiClient.ts`)

كل استجابة تمرّ بـ Zod قبل أن تلمس أي مكوّن:

```
axios → ظرف { success, data } → schema الميزة → النوع المضمون
```

`interface` مكتوبة يدوياً وعدٌ لا يستطيع المترجم الوفاء به وقت التشغيل؛ `z.infer` يعطي نفس النوع الثابت **بالإضافة** إلى تحقق فعلي من البايتات. تغيّر شكل الـ API يفشل في مكان واحد معروف بدل أن يُنتج `undefined` في عمق شجرة المكوّنات.

**تفصيلة تقنية مهمة:** نوع المعامل هو `ZodType<T, ZodTypeDef, unknown>` لا `ZodType<T>` — الأخير يُثبّت المدخل والمخرج على نوع واحد فيكسر أي schema يستخدم `.default()` أو `.transform()`. (هذا خطأ حقيقي ظهر أثناء البناء وتم إصلاحه.)

### 4.4 خطأ واحد فقط تراه الواجهة (`RequestError`)

Axios يرمي ثلاثة أشكال مختلفة، والباك اند له ظرفه الخاص. `apiClient` يحوّلها كلها إلى `RequestError` بمُصنِّفات:

```ts
error.isNetworkError   // status 0 — لم يصل للخادم
error.isRetryable      // شبكة أو 5xx → إعادة المحاولة منطقية
error.isNotFound       // 404
error.messageFor('phone')   // خطأ حقل من الباك اند → مدخل النموذج
```

`isRetryable` تُستهلَك في ثلاثة أماكن (سياسة React Query، زر إعادة المحاولة في `QueryState`، hook التتبع) — فلا يمكن أن تختلف الإجابة بينها.

### 4.5 طبقة السيو (`components/seo/Seo.tsx`)

مدير `<head>` مكتوب يدوياً (~90 سطراً، صفر تبعيات) بدل `react-helmet-async`.

**العقد:** `<Seo />` واحد لكل صفحة، في أعلى المكوّن. يكتب: `title`، `description`، `robots`، Open Graph كاملاً، Twitter Card، `canonical`، `hreflang` لكل لغة + `x-default`، و JSON-LD.

كل عنصر يكتبه يحمل `data-seo` ليُنظّف ما يملكه فقط ولا يتعارك مع الوسوم الثابتة في `index.html`.

**مصائد سيو مُعالَجة صراحةً:**
- صفحة 404 في SPA تُخدَم بـ HTTP 200 → لولا `noIndex` لفُهرس كل رابط مكسور («soft 404»).
- `canonicalPath()` يزيل `?utm=` والشرطة الأخيرة — `/about` و`/about/` و`/about?x` صفحة واحدة لا ثلاث.
- `organizationSchema` على الصفحة الرئيسية **فقط**؛ تكراره على كل صفحة يُميّع الكيان.
- صفحات نتيجة التتبع `noIndex` دائماً: عناوينها غير محدودة ومحتواها خاص بالمستخدم.

### 4.6 الموافقة تسبق التحليلات

`analytics.track()` لا يُرسل شيئاً قبل `setAnalyticsConsent(true)`. الأحداث المبكرة تُخزَّن في طابور (بحد 50) وتُفرَّغ عند الموافقة، أو تُمسح عند الرفض. لافتة الموافقة ليست ديكوراً — هي البوابة الفعلية.

أسماء الأحداث اتحاد أنواع (`AnalyticsEvent`)، فحدث مُعاد تسميته يكسر البناء بدل أن يشطر قمع تحويل بصمت.

### 4.7 الأمان في موقع عام

- **لا توكن ولا اعتراض مصادقة.** الموقع لا يملك جلسة ولا يجب أن يرسل اعتماديات أبداً.
- **مصيدة عسل (honeypot)** في نماذج التواصل: حقل مخفي بـ CSS (لا `type="hidden"` — البوتات تتخطاه). الحقل المملوء يُسقط الإرسال محلياً ويُرجع نجاحاً وهمياً؛ رسالة الخطأ كانت ستدعو البوت لإعادة المحاولة.
- **الحقل لا يُرسَل للخادم** — يُجرَّد في `contact.api.ts` كي لا تُكشف الحيلة.
- **مبدأ أقل امتياز في schema التتبع**: الحقول الحساسة غير موجودة أصلاً في الـ schema فتُسقَط حتى لو أرسلها الباك اند.

### 4.8 تقسيم الحزم

`manualChunks` بصيغة **دالة** لا كائن. الصيغة الكائنية تطابق مدخل الحزمة فقط، فـ `react-dom/client` وملفات الحزم الداخلية تتبع مستوردها — وهو ما جعل React يُدمج داخل حزمة الراوتر (206kB). بعد الإصلاح: `vendor-react` 142kB و`vendor-router` 64kB منفصلتين، وتعديل نص في صفحة لا يُبطل كاش المكتبات.

كل صفحة `lazy()`: زائر `/about` لا يحمّل كود صفحة التتبع.

---

## 5. تدفق البيانات (الشريحة المرجعية: `features/tracking/`)

```
TrackingPage.tsx          العرض فقط — لا يعرف axios ولا عناوين URL
      ↓ useTrackOrder(code)
useTrackOrder.ts          سياسة الكاش: enabled · staleTime · retry
      ↓ trackingApi.getByCode()
tracking.api.ts           الملف الوحيد الذي يذكر عنواناً لهذه الشريحة
      ↓ http.get(url, schema)
lib/http/apiClient.ts     تطبيع الأخطاء + تحقق Zod
      ↓
tracking.schema.ts        عقد البيانات — الأنواع مشتقة منه بـ z.infer
```

ثلاثة قرارات تعيش في الـ hook لا في الصفحة، ولذلك لا تتكرر:
- `enabled`: لا طلب لرمز يفشل التحقق المحلي، ولا طلب والعَلَم مطفأ → رمز مخمَّن يكلّف صفر طلبات.
- `retry: false` على 404: الرمز غير الموجود لن يوجد في المحاولة الثانية، والإعادة تبدو كتخمين آلي.
- `staleTime` قصير هنا (30 ثانية) خلافاً لافتراضي الموقع (5 دقائق): الطلب الجاري يتغيّر فعلاً.

---

## 6. الوصولية (مبنية من البداية)

مدمجة في البنية لأن إضافتها لاحقاً تعني إعادة كتابة الـ markup:

- **Skip link** إلى `#main-content` — أول عنصر قابل للتركيز.
- **معالم دلالية**: `header` / `main` / `footer` + `<nav aria-label>`.
- `NavLink` يضع `aria-current="page"` تلقائياً.
- `:focus-visible` عالمي بلون التمييز (لا `:focus` — النقر بالفأرة لا يترك حلقة).
- `role="status"` + `aria-live` على حالات التحميل، `role="alert"` على الأخطاء.
- `prefers-reduced-motion` مُحترَم عالمياً في `reset.css`.
- `ScrollToTop` يحترم الـ hash ولا يكسر التنقل بالمرساة.

---

## 7. الاختبارات

`src/test/architecture.test.tsx` يختبر **المعمارية لا المحتوى** — 13 اختباراً تمرّ:

- المسارات تُحلّ عبر كامل كومة الموفّرات، و404 يعمل.
- `document.title` و`link[rel=canonical]` يُكتبان فعلاً.
- سجل المسارات: لا تكرار، ولا مسار معطّل في القائمة أو الـ sitemap، ولا مسار بمعامل في الـ sitemap.
- تصنيف `RequestError` وربط أخطاء الحقول.
- أدوات URL وتطبيع canonical.
- schemas النماذج (تنسيق الهاتف العراقي، تطبيع رمز التتبع).

نص الصفحات يتغيّر بحرية دون لمس هذا الملف — وهذا هو المقصود.

> **ملاحظة من التنفيذ:** التهيئة تفرض العربية في `setup.ts` لأن i18n يرجع للغة المتصفح عند غياب اختيار محفوظ، و jsdom يبلّغ `en-US` — بدون ذلك كانت الاختبارات تعمل بالإنجليزية بصمت.

---

## 8. ما تبقّى (بترتيب الأولوية)

1. **الباك اند**: المسارات العامة الثلاثة + rate limiter خاص بها (تفاصيل في `lib/http/endpoints.ts`).
2. **التصميم**: ملفات `*.module.css` بجانب المكوّنات، مبنية على `tokens.css`.
3. ~~**المحتوى**: نصوص الصفحات، أسئلة `faq.content.ts`، نصّا الشروط والخصوصية.~~ — الشروط والخصوصية مكتملان (`features/legal/`)؛ تبقّى أسئلة `faq.content.ts`.
4. **الأصول**: `favicon.svg`، `logo.png`، `og/default.png` (1200×630)، أيقونات PWA.
5. **النشر و CI/CD**: راجع `DEPLOYMENT.md` — يتطلّب مستودع GitHub منفصل للموقع، وموقع Static جديد على CloudPanel، وضبط الأسرار/المتغيرات قبل أول تشغيل لـ `.github/workflows/website.yml`.
6. **الجودة**: `npm run lint` معرَّف في `package.json` لكن ESLint غير مثبَّت/مُهيَّأ بعد — لذلك مستبعَد من الـ CI حالياً.
7. **اختياري لاحقاً**: prerender/SSG للسيو الأقصى (المعمارية جاهزة — بناة JSON-LD دوال نقية وسجل المسارات قابل للتعداد).
